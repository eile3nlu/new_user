Credentials:
    .credentials.json (Secret Server) created by generate_creds.sh
    .client_secret.json (gmail) client ID and secret key created in Google APIs 


    need to update on creating google api secret
    need to update on creating jumpcloud api key
